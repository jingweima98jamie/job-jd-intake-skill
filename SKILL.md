---
name: job-jd-intake
description: 批量收录招聘 JD 截图并产出投递决策包。当用户丢来一张或多张岗位 JD 截图，说"处理这些工作/岗位截图""按求职流程收录""看看这几个岗能不能投"时使用：识别截图并查重建岗、建编号文件夹存档截图、做公司背调（是否外包/真实规模/口碑/风险）、输出背调与匹配度文档、判定投递与否、对要投的岗位生成定制单页简历（HTML+PDF）、更新岗位收录清单、按优先级汇报。运行前需要 profile.md（个人档案与写作铁律），缺失时会引导用户创建。
license: MIT
compatibility: 需要 Python 3.9+（pypdf）与 Chrome/Chromium（用于 HTML 转 PDF）；建议在支持读图与联网搜索的 Agent 环境运行
metadata:
  version: "1.0"
  author: "TODO 改成你的名字"
---

# 岗位 JD 截图收录

把用户丢来的一批招聘 JD 截图，变成"可决策的投递包"：每个岗位一份背调文档，要投的岗位再配一份按 JD 定制的单页简历。

## 第 0 步：读 profile.md（不可跳过）

在本技能目录（或用户指定的工作根目录）找 `profile.md`：

- **存在** → 读完再动手。里面是求职者本人的档案（基本信息、教育、履历事实库、项目、技能）、写作铁律、以及路径配置（工作根目录、简历母版位置）。
- **不存在** → 复制 `profile.example.md` 为 `profile.md`，通过提问帮用户填好（一次问 3-5 项，别一次问到底），填完再继续。

`profile.md` 是私有文件，已在 `.gitignore` 中排除，**绝不允许写进仓库或提交**。

## 流程

1. **读图识别**：逐张读截图，每张记下公司、岗位名、薪资（含几薪）、地点、经验/学历要求、招聘人数、发布/更新时间、HR 姓名头衔与活跃时间、渠道、平台标注的公司规模与融资标签、JD 全文要点。注意截图里的**信息面板**（规模/融资/HR 活跃/更新日期）是背调原料，别只抄 JD 正文。
2. **查重**：列表看已有岗位文件夹。同公司同岗的重挂（渠道不同、薪资微调）**不新建文件夹**，截图并入旧文件夹并在其背调文档头部补一行重挂记录。拿不准就问用户。
3. **编号**：新编号 = 现有最大编号 + 1；历史空号不回填。命名规则见 `references/判定规则.md`。
4. **建文件夹存图**：`工作N+岗位名（公司简称）`，截图重命名为 `工作N_JD_岗位名_序号.png`。存完 `ls` 确认。
5. **公司背调**：每家按 `references/检索关键词库.md` 的六个方向检索（主体股权/真实规模/口碑/风险/业务现状/直招还是代发），查不到本身就是结论。**"是不是外包"必须给明确判断**。
6. **写背调文档**：按 `references/背调模板.md` 八段模板写入岗位文件夹，核心是适配度表格（JD 要求 ↔ 履历逐条对应）、匹配度百分比和投递建议。用 `references/判定规则.md` 的档位锚定，不要拍脑袋。用户若追问"这种高薪岗怎么才能拿到"，追加成长路径章节（现实阶梯 + 6-12 个月动作，不建议横跳硬门槛差太远的赛道）。
7. **判定后再做简历**：只对判定"投"的岗位做。用 `scripts/make_resume.py`（规格驱动，禁止手搓排版），规格结构照抄 `assets/spec.example.json`，母版由用户在 profile.md 里指定（`assets/resume-template.example.html` 是空白起点）。内容改写原则：求职意向/一行卖点/个人总结按 JD 重写，把 JD 高频词自然嵌进"我做过的事"，**不得虚构任何经历或数字**。产出后校验 PDF **页数必须为 1**。
8. **更新岗位收录清单**：如果用户有技能汇总/复习清单这类文件，把新岗位的技能词并入交叉统计、给优质岗标注专属技能、每个新岗位追加一行"面试黑话速成"（行业黑话一句人话解释 + 面试叙事 + 注意事项），不投的岗位也要加行并写明原因。这一步不做完不算收录完成。
9. **汇报**：总结表（编号/岗位/公司/薪资/公司性质/匹配度/建议，按优先级降序）+ 关键提醒 3-5 条 + 下一步问题；用结果展示功能把背调文档和简历按优先级排给用户。

## 硬规则

- **不编造**任何经历、数字、工具熟练度；缺数字标【补充】留给用户填。
- 不写"正在学/在读"——要么会、要么不提；学过一点的东西表述为做过的事。
- 术语第一次出现必须带括号人话解释。
- 忌 AI 腔：不写"独立完成/一个人跑通/赋能闭环/踩坑"这类空话。
- 疑似虚假招聘（新注册 + 注册资本与标注规模矛盾 + 全网查无信息 + 中介代发 + 高薪远程 + "年限不限以过往成绩为准"）：**不投、不聊、不传简历**，背调文档加 `_高风险` 后缀写明理由与防范动作。
- 纯研发岗与运营背景错配时：不投不建简历，只做成长路径分析。
- 每一步真实验证（文件存在、页数=1、清单确实写入），**不许"应该写进去了"**。

## 参考文件

| 文件 | 用途 |
|---|---|
| `references/背调模板.md` | 八段制背调文档模板，直接复制改写 |
| `references/判定规则.md` | 编号命名规范、匹配度四档锚点、投/不投/高风险规则 |
| `references/检索关键词库.md` | 六个方向的检索词，直接套用 |
| `scripts/make_resume.py` | 简历生成器：规格 JSON → HTML → 单页 PDF（超页自动收紧） |
| `assets/spec.example.json` | 简历规格示例，照抄结构 |
| `assets/resume-template.example.html` | 空白简历母版，可当起点 |
| `profile.example.md` | 个人档案模板（复制成 profile.md 填自己的） |
